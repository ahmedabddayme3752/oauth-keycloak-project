<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>OAuth 2.0 Demo - Accueil</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        .hero-section {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
            padding: 100px 0;
        }
        .feature-card {
            transition: transform 0.3s ease;
        }
        .feature-card:hover {
            transform: translateY(-5px);
        }
    </style>
</head>
<body>
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
        <div class="container">
            <a class="navbar-brand" href="/">OAuth 2.0 Demo</a>
            <div class="navbar-nav ms-auto">
                <% if (isAuthenticated) { %>
                    <a class="nav-link" href="/dashboard">Dashboard</a>
                    <a class="nav-link" href="/profile">Profil</a>
                    <a class="nav-link" href="/logout">Déconnexion</a>
                <% } else { %>
                    <a class="nav-link" href="/login">Connexion</a>
                <% } %>
            </div>
        </div>
    </nav>

    <div class="hero-section">
        <div class="container text-center">
            <h1 class="display-4 mb-4">Démonstration OAuth 2.0</h1>
            <p class="lead mb-4">Implémentation complète avec Keycloak</p>
            <% if (isAuthenticated) { %>
                <p class="h5">Bienvenue, <%= user.preferred_username || user.name || 'Utilisateur' %>!</p>
                <a href="/dashboard" class="btn btn-light btn-lg mt-3">Accéder au Dashboard</a>
            <% } else { %>
                <a href="/login" class="btn btn-light btn-lg">Se connecter avec OAuth 2.0</a>
            <% } %>
        </div>
    </div>

    <div class="container my-5">
        <div class="row">
            <div class="col-md-4 mb-4">
                <div class="card feature-card h-100">
                    <div class="card-body text-center">
                        <div class="mb-3">
                            <i class="fas fa-shield-alt fa-3x text-primary"></i>
                        </div>
                        <h5 class="card-title">Sécurité</h5>
                        <p class="card-text">OAuth 2.0 fournit une authentification et autorisation sécurisées avec des tokens d'accès.</p>
                    </div>
                </div>
            </div>
            <div class="col-md-4 mb-4">
                <div class="card feature-card h-100">
                    <div class="card-body text-center">
                        <div class="mb-3">
                            <i class="fas fa-key fa-3x text-success"></i>
                        </div>
                        <h5 class="card-title">Keycloak</h5>
                        <p class="card-text">Serveur d'autorisation open-source qui implémente les standards OAuth 2.0 et OpenID Connect.</p>
                    </div>
                </div>
            </div>
            <div class="col-md-4 mb-4">
                <div class="card feature-card h-100">
                    <div class="card-body text-center">
                        <div class="mb-3">
                            <i class="fas fa-users fa-3x text-info"></i>
                        </div>
                        <h5 class="card-title">Gestion d'Utilisateurs</h5>
                        <p class="card-text">Centralisation de l'authentification et gestion des rôles et permissions.</p>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <footer class="bg-dark text-white text-center py-4">
        <div class="container">
            <p>&copy; 2025 Projet Sécurité - OAuth 2.0 avec Keycloak</p>
        </div>
    </footer>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
    <script src="https://kit.fontawesome.com/your-fontawesome-kit.js" crossorigin="anonymous"></script>
</body>
</html>
