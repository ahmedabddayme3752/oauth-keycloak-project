<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Dashboard - OAuth 2.0 Demo</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        .dashboard-header {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
            padding: 60px 0;
        }
        .stat-card {
            transition: transform 0.3s ease;
        }
        .stat-card:hover {
            transform: translateY(-5px);
        }
        .user-info {
            background: #f8f9fa;
            border-radius: 10px;
            padding: 20px;
        }
    </style>
</head>
<body>
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
        <div class="container">
            <a class="navbar-brand" href="/">OAuth 2.0 Demo</a>
            <div class="navbar-nav ms-auto">
                <a class="nav-link" href="/dashboard">Dashboard</a>
                <a class="nav-link" href="/profile">Profil</a>
                <a class="nav-link" href="/logout">Déconnexion</a>
            </div>
        </div>
    </nav>

    <div class="dashboard-header">
        <div class="container">
            <h1 class="display-4">Dashboard</h1>
            <p class="lead">Bienvenue dans votre espace sécurisé</p>
        </div>
    </div>

    <div class="container my-5">
        <div class="row">
            <div class="col-md-8">
                <div class="card">
                    <div class="card-header">
                        <h5 class="card-title mb-0">Informations de l'Utilisateur</h5>
                    </div>
                    <div class="card-body">
                        <div class="user-info">
                            <div class="row">
                                <div class="col-md-6">
                                    <p><strong>Nom d'utilisateur:</strong> <%= user.preferred_username || 'N/A' %></p>
                                    <p><strong>Email:</strong> <%= user.email || 'N/A' %></p>
                                    <p><strong>Nom complet:</strong> <%= user.name || 'N/A' %></p>
                                </div>
                                <div class="col-md-6">
                                    <p><strong>Prénom:</strong> <%= user.given_name || 'N/A' %></p>
                                    <p><strong>Nom de famille:</strong> <%= user.family_name || 'N/A' %></p>
                                    <p><strong>Email vérifié:</strong> 
                                        <% if (user.email_verified) { %>
                                            <span class="badge bg-success">Oui</span>
                                        <% } else { %>
                                            <span class="badge bg-warning">Non</span>
                                        <% } %>
                                    </p>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>

                <div class="card mt-4">
                    <div class="card-header">
                        <h5 class="card-title mb-0">Ressource Protégée</h5>
                    </div>
                    <div class="card-body">
                        <p>Cette section démontre l'accès à une ressource protégée via OAuth 2.0.</p>
                        <button class="btn btn-primary" onclick="fetchProtectedResource()">Tester l'API Protégée</button>
                        <div id="api-result" class="mt-3"></div>
                    </div>
                </div>
            </div>

            <div class="col-md-4">
                <div class="card stat-card">
                    <div class="card-body text-center">
                        <i class="fas fa-shield-alt fa-3x text-success mb-3"></i>
                        <h5>Authentification</h5>
                        <p class="text-success">Connecté avec OAuth 2.0</p>
                    </div>
                </div>

                <div class="card stat-card mt-3">
                    <div class="card-body text-center">
                        <i class="fas fa-key fa-3x text-primary mb-3"></i>
                        <h5>Token d'Accès</h5>
                        <p class="text-primary">Valide et sécurisé</p>
                    </div>
                </div>

                <div class="card stat-card mt-3">
                    <div class="card-body text-center">
                        <i class="fas fa-user-check fa-3x text-info mb-3"></i>
                        <h5>Session</h5>
                        <p class="text-info">Active et sécurisée</p>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <footer class="bg-dark text-white text-center py-4">
        <div class="container">
            <p>&copy; 2024 Projet Sécurité - OAuth 2.0 avec Keycloak</p>
        </div>
    </footer>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
    <script>
        async function fetchProtectedResource() {
            const resultDiv = document.getElementById('api-result');
            resultDiv.innerHTML = '<div class="spinner-border text-primary" role="status"><span class="visually-hidden">Chargement...</span></div>';
            
            try {
                const response = await fetch('/api/protected');
                const data = await response.json();
                
                if (response.ok) {
                    resultDiv.innerHTML = `
                        <div class="alert alert-success">
                            <h6>Réponse de l'API:</h6>
                            <pre>${JSON.stringify(data, null, 2)}</pre>
                        </div>
                    `;
                } else {
                    resultDiv.innerHTML = `
                        <div class="alert alert-danger">
                            <h6>Erreur:</h6>
                            <p>${data.error}</p>
                        </div>
                    `;
                }
            } catch (error) {
                resultDiv.innerHTML = `
                    <div class="alert alert-danger">
                        <h6>Erreur de connexion:</h6>
                        <p>${error.message}</p>
                    </div>
                `;
            }
        }
    </script>
</body>
</html>
