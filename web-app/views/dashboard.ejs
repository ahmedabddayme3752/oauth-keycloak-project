<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Dashboard - OAuth 2.0 Demo</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="/css/style.css" rel="stylesheet">
</head>
<body>
    <nav class="navbar navbar-expand-lg navbar-dark navbar-modern fixed-top">
        <div class="container">
            <a class="navbar-brand" href="/">
                <i class="fas fa-shield-alt me-2"></i>OAuth 2.0 Demo
            </a>
            <div class="navbar-nav ms-auto">
                <a class="nav-link" href="/">
                    <i class="fas fa-home me-1"></i>Accueil
                </a>
                <a class="nav-link" href="/profile">
                    <i class="fas fa-user me-1"></i>Profil
                </a>
                <a class="nav-link" href="/logout">
                    <i class="fas fa-sign-out-alt me-1"></i>Déconnexion
                </a>
            </div>
        </div>
    </nav>

    <div class="dashboard-header">
        <div class="container text-center dashboard-content">
            <h1 class="display-4 mb-4">
                <i class="fas fa-tachometer-alt me-3"></i>Dashboard
            </h1>
            <p class="lead">Bienvenue dans votre espace sécurisé</p>
        </div>
    </div>

    <div class="container section-padding">
        <div class="row">
            <div class="col-md-8">
                <div class="info-card">
                    <h5 class="card-title">
                        <i class="fas fa-user-circle me-2"></i>Informations de l'Utilisateur
                    </h5>
                    <div class="user-info">
                        <div class="row">
                            <div class="col-md-6">
                                <p><strong><i class="fas fa-user me-2"></i>Nom d'utilisateur:</strong> <%= user.preferred_username || 'N/A' %></p>
                                <p><strong><i class="fas fa-envelope me-2"></i>Email:</strong> <%= user.email || 'N/A' %></p>
                                <p><strong><i class="fas fa-id-card me-2"></i>Nom complet:</strong> <%= user.name || 'N/A' %></p>
                            </div>
                            <div class="col-md-6">
                                <p><strong><i class="fas fa-user me-2"></i>Prénom:</strong> <%= user.given_name || 'N/A' %></p>
                                <p><strong><i class="fas fa-user me-2"></i>Nom de famille:</strong> <%= user.family_name || 'N/A' %></p>
                                <p><strong><i class="fas fa-check-circle me-2"></i>Email vérifié:</strong> 
                                    <% if (user.email_verified) { %>
                                        <span class="badge bg-success">Oui</span>
                                    <% } else { %>
                                        <span class="badge bg-warning">Non</span>
                                    <% } %>
                                </p>
                            </div>
                        </div>
                    </div>
                </div>

                <div class="info-card">
                    <h5 class="card-title">
                        <i class="fas fa-code me-2"></i>Ressource Protégée
                    </h5>
                    <p>Cette section démontre l'accès à une ressource protégée via OAuth 2.0.</p>
                    <button class="btn-modern" onclick="fetchProtectedResource()">
                        <i class="fas fa-play me-2"></i>Tester l'API Protégée
                    </button>
                    <div id="api-result" class="api-result" style="display: none;"></div>
                </div>
            </div>

            <div class="col-md-4">
                <div class="stat-card">
                    <div class="stat-icon security">
                        <i class="fas fa-shield-alt"></i>
                    </div>
                    <h5>Authentification</h5>
                    <p class="text-success">Connecté avec OAuth 2.0</p>
                </div>

                <div class="stat-card mt-3">
                    <div class="stat-icon token">
                        <i class="fas fa-key"></i>
                    </div>
                    <h5>Token d'Accès</h5>
                    <p class="text-primary">Valide et sécurisé</p>
                </div>

                <div class="stat-card mt-3">
                    <div class="stat-icon session">
                        <i class="fas fa-user-check"></i>
                    </div>
                    <h5>Session</h5>
                    <p class="text-info">Active et sécurisée</p>
                </div>
            </div>
        </div>
    </div>

    <footer class="footer-modern">
        <div class="container text-center">
            <div class="row">
                <div class="col-md-6">
                    <h5><i class="fas fa-graduation-cap me-2"></i>ENET'COM</h5>
                    <p>Projet Sécurité - OAuth 2.0 avec Keycloak</p>
                </div>
                <div class="col-md-6">
                    <h5><i class="fas fa-users me-2"></i>Étudiants</h5>
                    <p>Ahmed AbdDayme AHMEDBOUHA & Moussa Mahmoud BA</p>
                </div>
            </div>
            <hr class="my-4" style="border-color: #444;">
            <p class="mb-0">&copy; 2025 Projet Sécurité - OAuth 2.0 avec Keycloak</p>
        </div>
    </footer>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
    <script src="https://kit.fontawesome.com/a076d05399.js" crossorigin="anonymous"></script>
    <script>
        async function fetchProtectedResource() {
            const resultDiv = document.getElementById('api-result');
            resultDiv.style.display = 'block'; // Show the result div
            resultDiv.innerHTML = '<div class="text-center"><i class="fas fa-spinner fa-spin me-2"></i>Chargement...</div>';
            
            try {
                const response = await fetch('/api/protected');
                const data = await response.json();
                
                if (response.ok) {
                    resultDiv.innerHTML = `
                        <div class="alert alert-success">
                            <h6><i class="fas fa-check-circle me-2"></i>Réponse de l'API:</h6>
                            <pre style="background: #f8f9fa; padding: 1rem; border-radius: 5px; border-left: 4px solid #28a745;">${JSON.stringify(data, null, 2)}</pre>
                        </div>
                    `;
                } else {
                    resultDiv.innerHTML = `
                        <div class="alert alert-danger">
                            <h6><i class="fas fa-exclamation-triangle me-2"></i>Erreur:</h6>
                            <p>${data.error}</p>
                        </div>
                    `;
                }
            } catch (error) {
                resultDiv.innerHTML = `
                    <div class="alert alert-danger">
                        <h6><i class="fas fa-exclamation-triangle me-2"></i>Erreur de connexion:</h6>
                        <p>${error.message}</p>
                    </div>
                `;
            }
        }
    </script>
</body>
</html>
